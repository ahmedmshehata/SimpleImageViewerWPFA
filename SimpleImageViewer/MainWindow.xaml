<Window x:Class="SimpleImageViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SimpleImageViewer"
        mc:Ignorable="d" KeyDown="Window_KeyDown" BorderThickness="0" WindowStyle="None"
        MouseDown="Window_MouseDown" MouseDoubleClick="Window_MouseDoubleClick" WindowStartupLocation="CenterScreen"
        Title="MainWindow" Height="450" Width="800">
    <Window.ContextMenu>
        <ContextMenu>
            <MenuItem Header="Load Directory..." Click="MenuItem_LoadDirectory_Click"/>
            <MenuItem Header="Theme">
                <MenuItem Header="Light" Click="MenuItem_ThemeLight_Click"/>
                <MenuItem Header="Dark" Click="MenuItem_ThemeDark_Click"/>
            </MenuItem>
            <MenuItem Header="Exit" Click="MenuItem_Exit_Click"/>
        </ContextMenu>
    </Window.ContextMenu>
    <Grid>
        <Image x:Name="screenImage" Stretch="Uniform" />

        <!-- Loading overlay shown while images are enumerated/loaded -->
        <Grid x:Name="loadingOverlay" Visibility="Collapsed" Background="#80000000">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Vertical">
                <ProgressBar IsIndeterminate="True" Width="240" Height="18" />
                <TextBlock x:Name="foundCountText" Text="Found: 0" Foreground="White" HorizontalAlignment="Center" Margin="0,8,0,0" />
                <Button Content="Cancel" Width="100" Margin="0,12,0,0" Click="CancelLoad_Click" HorizontalAlignment="Center" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
